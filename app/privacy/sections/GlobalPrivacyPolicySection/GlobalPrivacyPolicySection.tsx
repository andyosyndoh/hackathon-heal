import React from "react";

export const GlobalPrivacyPolicySection = (): JSX.Element => {
  return (
    <section className="w-full">
      <h2 className="[font-family:'Public_Sans',Helvetica] font-bold text-black text-base tracking-[0] leading-[26.4px] mb-3">
        Global API Factory&#39;s API Hub
      </h2>

      <p className="[font-family:'Public_Sans',Helvetica] font-normal text-black text-base tracking-[0] leading-[26.4px]">
        Global API Factory&#39;s API Hub is the world&apos;s largest public API
        hub that enables developers to build, test, publish, and expose their
        APIs to a broad audience of API consumers who, in turn, use API Hub to
        discover, evaluate, integration, and manage suitable APIs for inclusion
        into solutions they are building. We have limited visibility into the
        type of data traversing an API. However, in the use of the Services, PII
        such as user name and IDs are captured by our systems. We ensure that
        all data and information is treated with care. We also ensure that we
        have a clear reason for processing the data, that the data is held
        securely and disposed of when the purpose for which the data was
        originally collected is no longer necessary or the lawful basis for
        processing the data is no longer valid.
      </p>
    </section>
  );
};
