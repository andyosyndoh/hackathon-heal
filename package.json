{
  "name": "heal-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node dev-setup.js",
    "dev:next": "cd frontend && npm run dev",
    "dev:netlify": "npx netlify dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npm run lint"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "netlify-cli": "^17.0.0"
  }
}
